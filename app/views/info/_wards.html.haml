%h2= wards_title

%ul
  - for ward in wards
    %li
      = link_to ward.name, "#" + ward.name
      %span.grey= pluralize ward.candidates.size, 'candidate'

- for ward in wards
  %a{:name => ward.name}
  = render :partial => 'ward', :locals => { :ward => ward}
